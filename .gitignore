# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
CLAUDE.md
.claude/
docs/design-brief-assets/
docs/design-handovers/

# Wiki (Claude Wiki: local + Obsidian mirror, not committed)
wiki/
raw/

# Env / secrets
.env
.env.*
!.env.example

# Vite build output (Cloudflare builds this)
dist/
scripts/parity/out/

# Local wrangler state (miniflare's KV store for `wrangler pages dev`)
.wrangler/

# Local Function env vars. The .env patterns above do NOT cover this filename.
# (Note: `wrangler pages dev` 4.118 does not actually deliver these to a
# Function's env — see functions/api/_access.ts — but the file must never be
# committed regardless.)
.dev.vars
